## 1. Implementation

- [x] 1.1 Add `--config` CLI option and surface it in help text
- [x] 1.2 Implement YAML config loader and validation for supported fields
- [x] 1.3 Merge YAML config with CLI options using CLI precedence
- [x] 1.4 Manual validation: run CLI with config-only and config+CLI overrides
